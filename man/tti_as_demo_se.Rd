% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se.R
\name{tti_as_demo_se}
\alias{tti_as_demo_se}
\title{A small TreeSummarizedExperiment for examples}
\usage{
tti_as_demo_se()
}
\value{
A \code{TreeSummarizedExperiment} with one assay, \code{"clr"}.
}
\description{
Wraps \link{taxa_demo} in a \code{TreeSummarizedExperiment}, with \code{subject} and
\code{timepoint} columns in \code{colData}, so the container interface can be
demonstrated without constructing an object by hand.
}
\details{
The demo table is missing the sample \code{S04.2} entirely. That column is
created here holding \code{NA}, so the returned object is rectangular and every
subject-timepoint has a place. Two further samples, \code{S02.1} and \code{S07.4},
are present but entirely \code{NA}.
}
\examples{
se <- tti_as_demo_se()
dim(se)
head(SummarizedExperiment::colData(se))

}
\seealso{
\link{taxa_demo}, \code{\link[=tti_run]{tti_run()}}
}
