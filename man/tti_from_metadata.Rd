% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_metadata.R
\name{tti_from_metadata}
\alias{tti_from_metadata}
\title{Build a design from an abundance table and its metadata}
\usage{
tti_from_metadata(
  abundance,
  metadata,
  sample_col,
  subject_col,
  time_col,
  taxon_col = NULL,
  verbose = TRUE
)
}
\arguments{
\item{abundance}{A data.frame or matrix with taxa in rows and samples in
columns. Sample names are taken from the column names.}

\item{metadata}{A data.frame with one row per sample.}

\item{sample_col}{Character name of the metadata column holding sample
identifiers. These must match the columns of \code{abundance}.}

\item{subject_col}{Character name of the metadata column identifying the
subject each sample was taken from.}

\item{time_col}{Character name of the metadata column holding the time
point. Values must be numeric or coercible to numeric.}

\item{taxon_col}{Character name of the column of \code{abundance} holding taxon
identifiers, or \code{NULL} (the default) to take them from the row names.}

\item{verbose}{Logical. Whether to report the design as it is built.}
}
\value{
An object of class \code{tti_design}: a list with the converted
\code{table}, the \code{map} from internal column names back to sample names, the
\code{metadata} extended with any subject-timepoint that has no sample,
\code{missing}, \code{observed}, \code{subjects}, \code{times} and \code{taxon_col}.
}
\description{
Takes data in the form a study produces — an abundance table with taxa in
rows and samples in columns, plus a metadata table saying which sample
belongs to which subject at which time — and prepares it for imputation.

The three metadata columns are named by argument, so they can be called
anything. \code{subject} refers to whatever the repeated measurements are taken
on: a mouse, a participant, a plot, a bioreactor.

What the design is missing is reported as it is found, and returned in the
result. Two kinds of gap are distinguished: a sample listed in the
metadata whose abundance column holds no data, and a subject-timepoint
with no sample at all, which is discovered from the grid of subjects
against time points.
}
\examples{
counts <- data.frame(
    taxon = c("Bacteroides", "Prevotella"),
    S1 = c(1.2, 0.4), S2 = c(1.5, 0.6), S3 = c(1.1, 0.5)
)
meta <- data.frame(
    sample = c("S1", "S2", "S3"),
    mouse = c("M01", "M01", "M02"),
    day = c(0, 7, 0)
)

design <- tti_from_metadata(
    counts, meta,
    sample_col = "sample", subject_col = "mouse", time_col = "day",
    taxon_col = "taxon"
)
design$missing

}
\seealso{
\code{\link[=tti_run]{tti_run()}}, which accepts the same arguments and returns results
under the original sample names.
}
